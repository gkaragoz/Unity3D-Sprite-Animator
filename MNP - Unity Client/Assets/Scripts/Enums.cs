public static class Enums {
    public enum AnimationState {
        Idle,
        Run
    }

    public enum Direction {
        Up,
        Down,
        Right,
        Left,
        UpRight,
        UpLeft,
        DownRight,
        DownLeft
    }
}
